@page "/disable-2fa"
@model Disable2faModel
@{
    ViewData["Title"] = "Bỏ xác thực 2 yếu tố (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>

<div class="alert alert-warning" role="alert">
    <p>
        <strong>Hành động này chỉ loại bỏ 2FA.</strong>
    </p>
    <p>
        Việc loại bỏ 2FA không thay đổi khóa được dùng trong việc xác thực ứng dụng. Nếu bạn muốn thay đổi khóa này
        được dùng trong xác thực ứng dụng bạn nên <a asp-page="./ResetAuthenticator">Đặt lại mã xác thực của bạn.</a>
    </p>
</div>

<div>
    <form method="post">
        <button class="btn btn-danger" type="submit">Loại bỏ 2FA</button>
    </form>
</div>
